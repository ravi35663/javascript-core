// Memoization
/*
    Memoization is a technique which reduce the unnecessary operation in the same function to increase the performance of the application.
    we pick the same data from the cache if it was present into the cache otherwise we calculate the data and add into the cache so that we can fetch it in future.
*/

function Memoization(data){
    let cache ={};
}